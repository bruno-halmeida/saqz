package br.com.saqz.sharedkernel

enum class ErrorCode {
    AUTHENTICATION_REQUIRED,
    EMAIL_NOT_VERIFIED,
    IDENTITY_PROVIDER_UNAVAILABLE,
    VALIDATION_FAILED,
    GROUP_NOT_FOUND,
    ACCESS_FORBIDDEN,
    ENTRY_REQUEST_NOT_FOUND,
    PRECONDITION_REQUIRED,
    VERSION_CONFLICT,
    INVITE_INVALID_OR_EXPIRED,
    INVITE_INVALID,
    INVITE_EXPIRED,
    INVITE_GROUP_DELETED,
    INVITE_ATTEMPT_LIMIT,
    ATTENDANCE_LINK_INVALID_OR_EXPIRED,
    ATTENDANCE_LINK_ATTEMPT_LIMIT,
    ATTENDANCE_LINK_UNAVAILABLE,
    PHOTO_INVALID,
    PHOTO_TOO_LARGE,
    PHOTO_NOT_FOUND,
    GAME_NOT_FOUND,
    GAME_SCHEDULE_CONFLICT,
    INVALID_GAME_TRANSITION,
    ATTENDANCE_DEADLINE_PASSED,
    ATTENDANCE_FROZEN,
    ACCOUNT_NOT_FOUND,
    ACCOUNT_SUSPENDED,
    PASSWORD_RESET_RATE_LIMIT,
    PASSWORD_RESET_CODE_INVALID,
    PASSWORD_RESET_CODE_EXPIRED,
    PASSWORD_RESET_ATTEMPT_LIMIT,
    PASSWORD_RESET_TOKEN_INVALID,
    GROUP_LIMIT_EXCEEDED,
    ATHLETE_LIMIT_EXCEEDED,
    SUBSCRIPTION_NOT_FOUND,
    SUBSCRIPTION_CONFLICT,
    SUBSCRIPTION_PENDING_CHECKOUT_MISMATCH,
    COUPON_NOT_FOUND,
    COUPON_EXPIRED,
    COUPON_ALREADY_REDEEMED,
    DOWNGRADE_BLOCKED,
    CARD_DECLINED,
}
