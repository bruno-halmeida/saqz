meta {
  name: Approve Entry Request
  type: http
  seq: 2
}

post {
  url: {{backendUrl}}/api/groups/{{groupId}}/entry-requests/{{userId}}/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{idToken}}
}

tests {
  test("entry request becomes a membership", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
