meta {
  name: List My Groups
  type: http
  seq: 8
}
get {
  url: {{backendUrl}}/api/groups
  body: none
  auth: bearer
}
auth:bearer { token: {{idToken}} }
tests {
  test("returns the groups the user has access to", function () { expect(res.status).to.equal(200); expect(res.body.groups).to.be.an("array"); });
}
