meta {
  name: Finance Totals
  type: http
  seq: 5
}
get {
  url: {{baseUrl}}/api/groups/{{groupId}}/finance/totals
  body: none
  auth: bearer
}
auth:bearer { token: {{idToken}} }
tests {
  test("manual totals only", function () { expect(res.status).to.equal(200); expect(res.body).not.to.have.property("balanceCents"); });
}
