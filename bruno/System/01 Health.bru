meta {
  name: Health
  type: http
  seq: 1
}

get {
  url: {{backendUrl}}/actuator/health
  body: none
  auth: none
}

tests {
  test("backend is healthy", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().status).to.equal("UP");
  });
}
